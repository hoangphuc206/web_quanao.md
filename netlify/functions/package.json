{
  "name": "f-v-payment-project",
  "version": "1.0.0",
  "description": "Payment integration for f&v project",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "moment-timezone": "^0.5.45"
    // Thêm các thư viện khác nếu bạn dùng, ví dụ:
    // "node-fetch": "^2.6.1"
  }
}
